[package]
name = "quake-util"
version = "0.1.0"
authors = ["Seth <rader.seth@gmail.com>"]
edition = "2021"
license = "CC0-1.0 OR MIT"

[features]
default = ["std"]
core_fills = ["hashbrown", "cstr_core"]
std = []

[dependencies]
hashbrown = { version = "^0.11", optional = true }
cstr_core = { version = "^0.2", optional = true, features = ["alloc"] }
